---
id: '24.6.1'
title: Diagnose AUSH release gate hang with timed sub-gates
slug: diagnose-aush-release-gate-hang-with-timed-sub-gat
status: in_progress
priority: 1
created_at: '2026-04-27T05:23:26.026617Z'
updated_at: '2026-04-27T05:34:06.689226Z'
acceptance: Each 24.6 release gate step has an independent timed result recorded; the hang culprit is identified or the full gate is replaced with bounded commands that complete; no external publish/tag/push occurs.
notes: |-
  ---
  2026-04-27T05:34:06.689221+00:00
  Timed sub-gates results recorded in /tmp/aush-release-gate-diagnosis.md. Findings: cargo test --no-run pass 20s; cargo test --lib --bins initially failed on lexer braced variable prefix removal due comment stripping of `#` inside `${VAR#prefix}`. Fixed in src/lexer/mod.rs by preserving `#` while inside braced variable expansion; rerun passed 9s. Release build passed 44s. Smoke test `tests/smoke_test.sh ./target/release/aush` passed 1s. Archive sanity for `aush` + `aushd` passed 2s. Package list check passed 366 files. Publish dry-run passed 20s and reports Packaging/Verifying aush. Full `cargo test` with 300s timeout did not hang; it failed quickly on existing command substitution assignment behavior: `tests/command_substitution_tests.rs::test_command_substitution_in_assignment` expected VAR=myvalue but got VAR='echo myvalue'. This is a real release gate blocker if requiring full cargo test.
labels:
- aush
- release
- diagnosis
- hang
parent: '24.6'
verify: cd /Users/asher/rush && test -f /tmp/aush-release-gate-diagnosis.md && rg -n 'cargo test|cargo build --release --bins|smoke_test|archive|cargo package|cargo publish' /tmp/aush-release-gate-diagnosis.md
checkpoint: '52436fb7a12b707934e1a87c08da4f791965f840'
verify_hash: d3a64d204374884980fc1a6ee667bfa012cbbe92710137a8eedd252c592f0d09
claimed_by: imp
claimed_at: '2026-04-27T05:27:06.031841Z'
kind: task
attempt_log:
- num: 1
  outcome: abandoned
  agent: imp
  started_at: '2026-04-27T05:27:06.031841Z'
---

24.6 monolithic verify hung for ~40 minutes and user canceled it. Do not retry the full gate unchanged. Diagnose by running each release gate step independently with explicit timeouts and record which step hangs/fails.

Sub-gates to run from /Users/asher/rush:
1. `cargo test` with a bounded timeout; if it hangs, rerun with `-- --nocapture` or narrower test targets to identify the test binary.
2. `cargo build --release --bins` with timeout.
3. `tests/smoke_test.sh ./target/release/aush` with timeout.
4. Archive sanity: create temp tar from `target/release/aush` and `target/release/aushd`, extract, run `aush --no-rc -c 'echo archive-ok'`, and run a daemon-safe help/version command for `aushd` with timeout.
5. `cargo package --list --allow-dirty` and package-content grep with timeout.
6. `cargo publish --dry-run --allow-dirty` with timeout.

For each sub-gate, append notes to 24.6 with command, pass/fail/hang, elapsed time, and any narrowed culprit. After diagnosis, update 24.6 verify to a reliable bounded release gate or create targeted blocker units for failures. Do not publish, tag, push, or update external registries.
