---
id: '25.2'
title: Refactor benchmark driver around Rush-native workload scripts
slug: refactor-benchmark-driver-around-rush-native-workl
status: open
priority: 1
created_at: '2026-04-24T08:48:09.832495Z'
updated_at: '2026-04-24T08:48:09.832495Z'
labels:
- benchmarks
- driver
- bash
- aush
parent: '25'
verify: cd /Users/asher/rush && test -f scripts/benchmark.sh
verify_timeout: 60
kind: job
---

Refactor or replace the current benchmark driver so it orchestrates comparisons over Rush-native workload scripts instead of embedding Bash-only shell programs.

Scope:
- Review current `scripts/benchmark.sh`
- Either adapt it or create a new driver that:
  - builds the release binary
  - invokes Rush-native workload scripts under Rush
  - invokes equivalent workloads under Bash/Zsh where sensible
  - records results cleanly

Requirements:
- The driver itself may remain a Bash script.
- It should clearly separate orchestration from the shell workloads being measured.
- Avoid shell syntax in the driver that gets mistaken for Rush workload content.

Dependencies:
- Depends on the first Rush-native workload scripts existing.

Verification:
- Driver runs and references workload scripts rather than embedding all benchmark logic inline
