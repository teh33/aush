---
id: '24.3'
title: Complete AUSH-first runtime env and path compatibility migration
slug: complete-aush-first-runtime-env-and-path-compatibi
status: closed
priority: 2
created_at: '2026-04-24T16:33:43.847949Z'
updated_at: '2026-04-24T16:52:28.461651Z'
acceptance: Runtime reads AUSH-prefixed env vars/paths first and falls back to existing RUSH vars/paths where needed; targeted tests prove both new and legacy names work for migrated surfaces.
notes: |-
  ---
  2026-04-24T16:39:29.548313+00:00
  Starting next rebrand phase after user-facing copy pass: AUSH-first runtime env/path compatibility. Will preserve legacy RUSH fallbacks and avoid package/binary/internal symbol rename.

  ---
  2026-04-24T16:52:28.461643+00:00
  Implemented AUSH-first runtime compatibility for several RUSH-only surfaces: suggestion env vars now use AUSH_SUGGEST_* with RUSH fallback; table/color env vars now use AUSH_NO_COLOR/AUSH_TABLE_STYLE/AUSH_COLOR_HEADER with RUSH fallback; run_api sets AUSH_JSON plus legacy RUSH_JSON and reads AUSH_MAX_OUTPUT with RUSH fallback; executor command substitution cap reads AUSH_MAX_SUBST_OUTPUT with RUSH fallback; AI tool autorun reads AUSH_AI_AUTORUN with RUSH fallback; status login check reads AUSH_LOGIN with RUSH fallback; Lua scripts now prefer ~/.aush/lua with ~/.rush/lua fallback; funcsave/funced prefer ~/.config/aush/functions with ~/.config/rush/functions fallback; rushd pool env vars now support AUSH_DISABLE_POOL/AUSH_POOL_SIZE with RUSH fallback and user-facing daemon copy says AUSH. Also restored missing approval_prompt helper needed by existing command metadata code after cargo check exposed it.

  Verification passed: cargo fmt --check, cargo check -q, cargo test -q correction::tests --lib, cargo test -q lua::tests --lib, cargo test -q command_metadata --lib, cargo test -q status --lib.

  Remaining targeted scan hits are intentional legacy fallback docs/tests or deferred compatibility surfaces: RUSH_* test preservation in correction tests; ~/.rush fallback docs in runtime/lua/daemon/functions; daemon socket legacy fallback; provider docs for ~/.rush/ai.toml; brand legacy helper.
labels:
- aush
- runtime-compat
- env
- paths
parent: '24'
verify: cd /Users/asher/rush && rg -n 'env::var\("RUSH_|std::env::var\("RUSH_|\.config/rush|\.rush/|rush-undo|rush\.sock|pi-rush' src || true && cargo check -q
checkpoint: a12f5cbf0c9bac9d03bb297fa16e2c5517aa69f9
verify_hash: cd6cdeadb5b2228ad98cca3190042f64ba8cca25a264f05838323c41657433b7
claimed_by: imp
claimed_at: '2026-04-24T16:39:20.755595Z'
kind: task
attempt_log:
- num: 1
  outcome: abandoned
  agent: imp
  started_at: '2026-04-24T16:39:20.755595Z'
---

Goal: complete runtime compatibility migration to AUSH-prefixed env vars and paths while preserving RUSH-prefixed fallback behavior.

Current state: `src/brand.rs` provides helpers for env var fallback and config path fallback. Several surfaces already use AUSH-first fallback (`AUSH_BANNER_*`, `AUSH_AI_*`, `AUSH_PROMPT*`, universal vars, daemon socket lookup, history/undo paths). Raw scan still shows additional runtime surfaces using RUSH-only env vars/paths.

Steps:
1. Inspect `reports/aush-phase1-rename-audit.md` and run targeted scan over `src/**` for `RUSH_`, `.rush`, `.config/rush`, `rush.sock`, `pi-rush`, `rush-undo`.
2. Convert real runtime env reads to `crate::brand::env_var` or `env_flag` with AUSH primary and RUSH legacy fallback.
3. For shell-set transition variables, consider setting both AUSH_* and RUSH_* when low risk and needed for compatibility.
4. Convert user-state paths to AUSH primary with legacy fallback when existing Rush state may exist.
5. Update comments/docs near touched code to describe AUSH primary + legacy Rush fallback.
6. Add or update focused tests only where behavior is not trivially covered.

Likely files:
- src/correction/mod.rs (RUSH_SUGGEST_* -> AUSH_SUGGEST_* fallback)
- src/value/render.rs and src/executor/value/render.rs (RUSH_NO_COLOR/TABLE/COLOR vars)
- src/run_api.rs (AUSH_JSON/AUSH_MAX_OUTPUT handling if appropriate)
- src/executor/mod.rs (RUSH_MAX_SUBST_OUTPUT legacy fallback currently duplicated)
- src/bin/rushd.rs (daemon env/config help text; binary name may wait for packaging task)
- src/lua/mod.rs (paths under ~/.rush/lua may need AUSH primary + fallback)
- src/builtins/funcsave.rs, src/builtins/funced.rs, src/builtins/abbr.rs (config/function paths)

Out of scope:
- Renaming internal types/protocols
- Binary/package rename
- Broad docs copy pass
- Removing legacy RUSH support

Do not blindly replace tests that intentionally assert legacy variables; add AUSH coverage instead where useful.
