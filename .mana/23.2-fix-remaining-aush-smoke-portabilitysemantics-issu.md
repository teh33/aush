---
id: '23.2'
title: Fix remaining AUSH smoke portability/semantics issues in tests/smoke_test.sh
slug: fix-remaining-aush-smoke-portabilitysemantics-issu
status: open
priority: 1
created_at: '2026-04-24T08:39:00.008369Z'
updated_at: '2026-04-24T08:39:00.008369Z'
labels:
- aush
- smoke
- tests
parent: '23'
verify: cd /Users/asher/rush && tests/smoke_test.sh ./target/release/rush >/tmp/aush-smoke-next.log 2>&1; tail -n 20 /tmp/aush-smoke-next.log
verify_timeout: 300
kind: job
---

AUSH fast smoke still fails due to issues inside tests/smoke_test.sh rather than harness timing.

Current evidence:
- The portable absolute-path fix improved one failure, but the smoke script still has quoting/semantics problems.
- The 'successful command' case became `true; echo 1` because `$?` was expanded by the outer shell when building the test command string in bash.
- Need to verify whether `nonexistent_cmd_12345 2>/dev/null; echo $?` should produce 127 in Rush or whether command-not-found currently aborts the command list before `echo $?` runs.
- `git log --oneline -1` still fails in this repo even though the repo has commits, so that is either a git builtin bug or an environment expectation mismatch.

Goal:
- Keep scope on smoke-test correctness and portability first.
- Fix shell-script quoting so `$?` is evaluated by Rush, not by the outer bash harness.
- Re-evaluate the remaining failures and isolate whether they are real product bugs.

Verification:
- `tests/smoke_test.sh ./target/release/rush` shows fewer failures than before
- `bash ./benches/aush_smoke_fast.sh ./target/release/rush` reflects the reduced failure set
