---
id: '25.4'
title: Make benchmark.sh run under Rush as an AUSH compatibility fixture
slug: make-benchmarksh-run-under-rush-as-an-aush-compati
status: open
priority: 1
created_at: '2026-04-24T08:54:19.804903Z'
updated_at: '2026-04-24T15:06:57.892154Z'
notes: |-
  ---
  2026-04-24T09:03:37.658211+00:00
  User clarified that both compatibility benchmark fixtures are present in-repo under `benches/benchmark.sh` and `benches/shellbench.sh`. Next compatibility pass should inspect and run those exact files under Rush rather than assuming only `scripts/benchmark.sh` exists.

  ---
  2026-04-24T09:04:20.508603+00:00
  Inspected the real fixture files under `benches/` and ran them under Rush.

  Current fixture results:
  - `./target/release/rush --no-rc benches/benchmark.sh` -> parser error at position 6352
  - `./target/release/rush --no-rc benches/shellbench.sh` -> parser error at position 6198

  Confirmed first concrete blockers:
  1. `benches/benchmark.sh`
     - Fails on a single-quoted lone backslash literal in:
       `test_output "escape backslash" '\\' sh -c 'echo \\\\'`
     - This suggests Rush mishandles a single-quoted string whose content is just a backslash.
  2. `benches/shellbench.sh`
     - Fails on a backslash line continuation inside a long argument list:
       `/bin/true a b c ... z \`
     - Confirms the broader backslash-newline continuation compatibility gap on a real fixture file.

  Revised compatibility punch list order:
  - First: support backslash-newline line continuation (broadest leverage, hits shellbench and likely many real scripts)
  - Second: fix parsing of single-quoted lone backslash literals (needed by benchmark.sh)

  These are now durable AUSH compatibility targets grounded in actual benchmark fixture files.

  ---
  2026-04-24T15:06:57.892147+00:00
  Plan update before continuing:
  - Treat `benches/benchmark.sh` and `benches/shellbench.sh` as the durable compatibility fixtures; the old `scripts/benchmark.sh` reference is stale.
  - Execute child 25.4.1 first because it targets the first/highest-leverage blocker: POSIX backslash-newline line continuation in real scripts.
  - After 25.4.1 verification, rerun both fixtures under `./target/release/rush --no-rc` and record the next concrete blocker for each.
  - Known downstream blockers from prior notes: `benches/shellbench.sh` now reaches `Expected command name`; `benches/benchmark.sh` now reaches a Unicode en dash lexer blocker (`Invalid token ... '–'`).
  - Do not broaden this unit into benchmark architecture changes; keep using fixture progress as the acceptance signal.
labels:
- aush
- compatibility
- benchmarks
- shell-scripts
parent: '25'
verify: cd /Users/asher/rush && ./target/release/rush --no-rc scripts/benchmark.sh >/tmp/benchmark-fixture.log 2>&1 || true && test -s /tmp/benchmark-fixture.log
verify_timeout: 120
kind: job
---

Make the existing generated benchmark scripts run under Rush as compatibility fixtures for the AUSH standard.

Context:
- User explicitly wants Rush/AUSH to be able to run plausible benchmark scripts written by competent shell users, even when those scripts were initially authored with Bash semantics in mind.
- `scripts/benchmark.sh` currently fails under Rush with a parser error on backslash line continuations.
- Likely additional blockers include `&>` redirection, brace expansion, and any other Bashisms encountered during execution.
- A separate benchmark-architecture improvement effort exists, but this unit is specifically about compatibility/product readiness.

Goal:
- Use the benchmark scripts themselves as acceptance fixtures.
- Make `scripts/benchmark.sh` run successfully under Rush, or produce a precise blocker list if a bounded fix is not yet done.
- If `shellbench.sh` is later provided, add it to the same fixture set and extend the blocker list.

Concrete steps:
1. Re-run `scripts/benchmark.sh` under Rush and capture the first failing syntax/semantic blocker.
2. Inspect the relevant parser/runtime code path.
3. Implement the narrowest compatibility fix that addresses the blocker without broad unrelated refactoring.
4. Re-run the script, capture the next blocker, and decide whether to continue or externalize follow-up units.
5. Record each confirmed blocker and outcome in mana so the script becomes a durable compatibility target.

Current confirmed blocker:
- Backslash line continuation in `scripts/benchmark.sh` causes parser failure (`Invalid token ... '\'`).

Verification:
- `./target/release/rush --no-rc scripts/benchmark.sh` progresses past the current parser failure, ideally to successful execution or to the next confirmed blocker with updated notes.

Constraints:
- Keep changes narrow and evidence-driven.
- Prefer compatibility fixes that align with plausible shell-user expectations.
- Do not replace the script with a different benchmark architecture in this unit; treat the script as the fixture.
