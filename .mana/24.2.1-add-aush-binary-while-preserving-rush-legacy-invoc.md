---
id: '24.2.1'
title: Add AUSH binary while preserving Rush legacy invocation
slug: add-aush-binary-while-preserving-rush-legacy-invoc
status: closed
priority: 2
created_at: '2026-04-24T16:59:34.669480Z'
updated_at: '2026-04-24T17:09:31.921415Z'
notes: |-
  ---
  2026-04-24T17:04:18.502143+00:00
  Starting additive binary rename: add primary `aush` executable while preserving legacy `rush` binary. Will avoid library/internal/Lua API renames.

  ---
  2026-04-24T17:09:31.921399+00:00
  Implemented additive binary rename: Cargo now builds primary `aush` binary from src/main.rs while keeping legacy `rush` binary from the same entrypoint; default-run changed to `aush`. First attempted a copied src/bin/aush.rs, but that failed because src/main.rs uses local modules relative to crate root; corrected to have both bin targets point at src/main.rs instead. Verification passed: cargo build -q --bins; target/debug/aush and target/debug/rush both exist; both --help commands render AUSH help; cargo fmt --check; cargo check -q.
labels:
- aush
- packaging
- binary
- compatibility
parent: '24.2'
verify: cd /Users/asher/rush && cargo build -q --bins && test -x target/debug/aush && test -x target/debug/rush && ./target/debug/aush --help >/tmp/aush-help.txt && ./target/debug/rush --help >/tmp/rush-help.txt
checkpoint: a12f5cbf0c9bac9d03bb297fa16e2c5517aa69f9
verify_hash: f664cfeb4681350a642c7badc5491d77b93ece5720492741bb2e36600f923bec
claimed_by: imp
claimed_at: '2026-04-24T17:04:10.164426Z'
kind: task
attempt_log:
- num: 1
  outcome: abandoned
  agent: imp
  started_at: '2026-04-24T17:04:10.164426Z'
---

Goal: make the project build a primary `aush` executable while keeping `rush` as a legacy compatibility invocation.

Current state: Cargo.toml still declares package/lib/bin names as `rush`; docs/help now prefer AUSH, but binary output remains `target/.../rush`. This first packaging step should be additive and compatibility-preserving.

Steps:
1. Inspect Cargo.toml bin/lib/package naming and current src/main.rs entrypoint.
2. Add an `aush` binary that uses the existing shell entrypoint without removing the existing `rush` binary.
3. Keep the legacy `rush` binary building from the same implementation for now.
4. Update minimal help/version text only if needed to avoid binary-name confusion.
5. Do not rename the Rust library crate or internal `rush::` imports in this unit unless unavoidable.
6. Run both new and legacy binary smoke checks.

Files likely in scope:
- Cargo.toml
- src/main.rs and/or a small src/bin wrapper if needed
- docs/INSTALLATION.md or README.md only if a tiny command example needs immediate correction

Out of scope:
- Homebrew formula rename
- CI/release artifact rename
- broad test/bench path updates
- Lua `rush.*` API rename
- internal module/type renames

Do not remove `rush` compatibility in this unit.
