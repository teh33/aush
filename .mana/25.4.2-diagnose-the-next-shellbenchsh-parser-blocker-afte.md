---
id: '25.4.2'
title: Diagnose the next shellbench.sh parser blocker after line-continuation support
slug: diagnose-the-next-shellbenchsh-parser-blocker-afte
status: open
priority: 1
created_at: '2026-04-24T14:50:12.724026Z'
updated_at: '2026-04-24T15:22:25.853553Z'
notes: |-
  ---
  2026-04-24T15:12:44.548946+00:00
  Reproduced current downstream blockers after closing 25.4.1:
  - `./target/release/rush --no-rc benches/shellbench.sh` -> `benches/shellbench.sh: Expected command name`
  - `./target/release/rush --no-rc benches/benchmark.sh` -> `benches/benchmark.sh: Invalid token at position 9106: '–'`

  Located approximate fixture contexts:
  - shellbench position 6198 / line ~319 is in the `benchmark "Many arguments" sh -c '...' 1000` block, after a multi-line single-quoted `sh -c` script containing backslash line continuations in a long `/bin/true ...` argument list.
  - benchmark position 9106 / line ~226 is a Unicode en dash in a comment: `# 14) Globbing (simple – no files created, just test expansion in /tmp)`. This suggests the lexer is tokenizing comments too narrowly or not skipping non-ASCII comment text safely.

  Next likely narrow investigations:
  1. For shellbench: build a minimal repro around `benchmark "Many arguments" sh -c '...multi-line single-quoted body...' 1000` and determine if the parser loses command separation after the closing quote or mishandles line continuations within single-quoted script arguments.
  2. For benchmark: make comment lexing skip from `#` to newline regardless of Unicode content, or otherwise allow UTF-8 comment text without token errors.

  ---
  2026-04-24T15:22:25.853523+00:00
  Implemented a narrow lexer fix for the benchmark fixture's Unicode comment blocker:
  - Added `strip_comments()` preprocessing in `Lexer::tokenize()` that removes `#...newline` comments outside single/double quotes before logos tokenization.
  - Kept quoted `#` content intact.
  - Left the existing logos comment skip rule but raised priority; the preprocessing is what handles non-ASCII comment text robustly.
  - Added targeted test `test_comment_with_unicode_text_is_skipped`.

  Verification:
  - `cargo test test_comment_with_unicode_text_is_skipped --quiet` passes.
  - `cargo test lexer --quiet` passes.
  - `cargo build --release --quiet` passes.

  Fixture progress after rebuild:
  - `benches/benchmark.sh` no longer fails on Unicode en dash comment. It now advances to `Invalid token at position 5485: '$('`, around line ~130 in recursive function body: `a=$(( $1 * $(fact $(( $1 - 1 ))) )); ...`.
  - `benches/shellbench.sh` remains at `Expected command name` around the many-arguments fixture; still needs focused parser diagnosis.

  Caution: `strip_comments` is intentionally simple and tracks single/double quotes plus escaped chars in double quotes. It does not fully parse command substitutions; future refinements may be needed if comments inside nested constructs reveal edge cases.
labels:
- aush
- compatibility
- parser
- shell-scripts
parent: '25.4'
verify: cd /Users/asher/rush && ./target/release/rush --no-rc benches/shellbench.sh >/tmp/shellbench-next.log 2>&1 || true && tail -n 20 /tmp/shellbench-next.log
verify_timeout: 180
kind: task
---

Investigate the next downstream parser/compatibility blocker exposed after fixing backslash-newline line continuation.

Context:
- After the lexer fix for line continuation and the custom single-quoted-string parser, the original `25.4.1` scope is satisfied.
- `benches/shellbench.sh` still fails, but no longer on the old continuation parse error. It now fails later with parser/runtime behavior around the `benchmark "Many arguments" sh -c ' ... '` fixture section.
- `benches/benchmark.sh` also advances past the old quoted-backslash issue and now fails later on a Unicode en dash token.

Goal:
- Treat the next `shellbench.sh` failure as a new bounded issue rather than extending `25.4.1` indefinitely.
- Determine whether the new failure is in parsing multi-line single-quoted arguments, command parsing after those arguments, or some later shell semantics.

Verification:
- Reproduce the current `shellbench.sh` failure on the latest release build and capture the exact next blocker with a minimal repro
