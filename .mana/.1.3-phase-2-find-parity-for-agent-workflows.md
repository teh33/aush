---
id: '.1.3'
title: 'phase 2: find parity for agent workflows'
slug: phase-2-find-parity-for-agent-workflows
status: open
priority: 1
created_at: '2026-04-04T18:58:18.296836Z'
updated_at: '2026-04-27T20:43:08.975029Z'
notes: |-
  ---
  2026-04-27T20:40:36.798003+00:00
  2026-04-27 manual-close audit plan: This broad find parity epic is only partially complete. Implemented explicit `-print` and `-quit` with targeted unit coverage. The original epic still names `-print0`, `-delete`, `-perm`, `-user`, `-group`, `-newer`, `-regex`, `-prune`, and `-exec ... +`; do not close the epic. Create or update child units for remaining flags if continuing parity work.

  ---
  2026-04-27T20:43:08.975028+00:00
  2026-04-27 decomposition: Partial implementation completed for explicit `find -print` and `find -quit` with targeted coverage. Keep this broad find parity epic open. Remaining parity should be split into children: output/traversal flags (`-print0`, `-prune`), destructive action (`-delete`, temp-dir tests only), predicates (`-perm`, `-user`, `-group`, `-newer`, `-regex`), and actions (`-exec ... +`).
labels:
- compatibility
- find
- agent-critical
parent: '.1'
verify: cargo test --quiet 2>/dev/null || cargo test
kind: epic
---

Expand find builtin toward GNU-compatible common flag parity for agent workflows. Prioritize -print0, -delete, -perm, -user, -group, -newer, -regex, -prune, and -exec ... + while preserving Rush-specific --json functionality.
